<form method="POST" action="{{ base_url + url_for('dashboard_bp.delete') }}" class="p-3">
    <div class="mb-3">
        {{ form.csrf_token }}
    </div>

    <div class="mb-3">
        {{ form.filename.label }}
        {{ form.filename(class="form-select") }}

        {% if form.filename.errors %}
        <ul>
            {% for error in form.filename.errors %}
            <li class="text-danger">{{ error }}</li>
            {% endfor %}
        </ul>
        {% endif %}
    </div>

    <div class="mb-3">
        {{ form.page.label }}
        {{ form.page(class="form-control") }}

        {% if form.page.errors %}
        <ul>
            {% for error in form.page.errors %}
            <li class="text-danger">{{ error }}</li>
            {% endfor %}
        </ul>
        {% endif %}
    </div>

    <div class="text-center">
        <div class="mb-3">
            {{ form.submit(class="btn btn-primary") }}
        </div>
    </div>
</form>