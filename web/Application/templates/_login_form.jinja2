<form method="POST" action="{{ base_url + url_for('auth_bp.login') }}" class="p-3">
    <div class="mb-3">
        {{ form.csrf_token }}
    </div>

    <div class="mb-3">
        {{ form.email.label }}
        {{ form.email(placeholder='youremail@example.com', class="form-control") }}
        
        {% if form.email.errors %}
        <ul>
            {% for error in form.email.errors %}
            <li class="text-danger">{{ error }}</li>
            {% endfor %}
        </ul>
        {% endif %}
    </div>

    <div class="mb-3">
        {{ form.password.label }}
        {{ form.password(class="form-control") }}
        
        {% if form.email.errors %}
        <ul>
            {% for error in form.password.errors %}
            <li class="text-danger">{{ error }}</li>
            {% endfor %}
        </ul>
        {% endif %}
    </div>

    <div class="text-center">
        <div class="mb-3">
            {{ form.submit(class="btn btn-primary") }}
        </div>
        
        <div class="mb-3">
            <span>
                Don't have an account?
                <a href="{{ base_url + url_for('auth_bp.register') }}">Sign up</a>.
            </span>
        </div>
    </div>
</form>